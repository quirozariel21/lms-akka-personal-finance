database {
  postgres {
    driver = "org.postgresql.Driver"
    url = "jdbc:postgresql://localhost:5432/finance-db"
    username = "postgres"
    password = "postgres"
    thread-pool-size = 32
  }
}